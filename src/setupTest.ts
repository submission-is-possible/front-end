import '@testing-library/jest-dom'; // Importa Jest-DOM per asserzioni aggiuntive
import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
    // Setup globale per i test
});

afterAll(() => {
    // Cleanup globale per i test
});
